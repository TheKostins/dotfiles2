# My personal dotfiles repo

## Zsh organization

Common zsh configuration lives in tracked modules under `zsh/.config/zsh`.
Machine-specific setup belongs in `zsh/.config/zsh/local.zsh`, which is ignored
by git. Use `zsh/.config/zsh/local.zsh.example` as a template.
